"""Swipe engine (domain): batches, votes, taste profile, prompts, scoring.

No HTTP and no vendor SDK here: the engine only talks to ``tindarr.ports`` and
``tindarr.storage``. Empty until step 4.
"""
